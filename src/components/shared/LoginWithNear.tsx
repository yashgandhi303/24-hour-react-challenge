import { Button } from '@components/ui';

const LoginWithNear = () => {
  return (
    <section className="end">
      <h3>Already have NEAR account?</h3>
      <Button black>Login with NEAR</Button>
    </section>
  );
};

export default LoginWithNear;
